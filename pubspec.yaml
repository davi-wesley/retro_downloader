name: retroDownloader
description: "Projeto Flutter Desktop para download de jogos retrô."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  rxdart: ^0.27.5
  shimmer: ^2.0.0
  data_table_2: ^2.5.12
  file_picker: ^8.0.3


  field_suggestion:
    git:
      url: https://github.com/pauloricardomsop/field_suggestion

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true